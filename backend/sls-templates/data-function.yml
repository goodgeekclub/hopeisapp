data:
  name: ${self:service}-data-${sls:stage}
  handler: dist/lambda.handler
  events:
    - http:
        method: GET
        path: /
        private: true
    - http:
        method: POST
        path: /
        private: true
    - http:
        method: ANY
        path: /data/{proxy+}
        private: true
