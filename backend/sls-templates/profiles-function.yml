profiles:
  name: ${self:service}-profiles-${sls:stage}
  handler: dist/main.handler
  events:
    - http:
        method: GET
        path: /profiles/{any+}
        private: true
    - http:
        method: POST
        path: /profiles/{any+}
        private: true
    - http:
        method: ANY
        path: /profiles/{any+}
        private: true
