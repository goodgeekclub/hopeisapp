profiles:
  name: ${self:service}-profiles-${sls:stage}
  handler: dist/lambda.handler
  events:
    - http:
        method: GET
        path: /
        private: true
    - http:
        method: POST
        path: /
        private: true
    - http:
        method: ANY
        path: /profiles/{proxy+}
        private: true
